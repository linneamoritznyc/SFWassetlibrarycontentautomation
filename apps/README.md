# apps

Things a person opens in a browser.

- `web` — Next.js 14 App Router on Vercel. Every screen (library, inbox, clips,
  week, scout, learned, questions, errors, settings) and every API route from
  backend spec section 8. It holds the service role key server-side only; the
  browser never sees it.
