# tests

`fixtures/` holds the sample material the phase acceptance tests run against: 30
photos, a 3-minute video with speech, and a PDF. They are deliberately git-ignored
because of size. `fixtures/README.md` says what to put there and where to get it.
