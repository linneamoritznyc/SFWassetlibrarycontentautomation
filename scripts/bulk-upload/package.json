{
  "name": "@sfw/script-bulk-upload",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@sfw/db": "workspace:*",
    "@sfw/queue": "workspace:*",
    "@sfw/storage": "workspace:*"
  }
}
