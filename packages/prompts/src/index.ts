// Seed prompt texts, one file per prompt, versioned. Loaded into the prompts
// table by the seed script; workers read the table, never these files.
// Filled in Phase 1.
export const PACKAGE_NAME = '@sfw/prompts';
